<br>
<div class="container">
	<table class="table table-responsive table-hover">
		<tbody>

			<tr>
		  	<td><strong>Name:</strong></td>
		  	<td><%= @user.name %></td>
		  </tr>

			<tr>
				<td><strong>Contact:</strong></td>
				<td><%= @user.contact %></td>
			</tr>

			<tr>
				<td><strong>Email:</strInfoong></td>
				<td><%= @user.email%></td>
			</tr>

			<tr>
				<% if @user.present? && current_user %>
				<%= link_to 'Edit', edit_user_registration_path(@user), class: "btn btn-danger" %>
				<% end %>
				<%= link_to 'Delete', [:admin, @user], method: :delete, data: { confirm: 'Are you sure to delete this User?' }, class: "btn btn-warning" %> 
				<%= link_to 'Back', admin_users_path, class: "btn btn-info" %> 
			</tr>
		</tbody>
	</table>
</div>
