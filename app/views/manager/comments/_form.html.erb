<%= form_for [:manager, @client, @project, @project.comments.build], remote: true do |f| %>

		<h2>
			<%= f.label :Comments %>
		</h2>
			<%= f.text_area :content, required: true %>
			<%= f.submit class: "btn btn-primary btn-sm" %><br><br>

<% end %> 
